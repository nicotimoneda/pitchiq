[project]
name = "pitchiq"
version = "0.1.0"
description = "Análisis táctico de equipo sobre StatsBomb Open Data: métricas computadas, no inventadas"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "statsbombpy>=1.14",
    "pandas>=2.0",
    "numpy>=1.26",
    "mplsoccer>=1.4",
    "pydantic>=2.0",
    "scipy>=1.17.1",
    "langgraph>=1.2.7",
    "langchain-core>=1.4.8",
    "anthropic>=0.116.0",
    "qdrant-client>=1.18.0",
    "sentence-transformers>=5.6.0",
    "torch>=2.0",  # directa para poder fijar la build CPU en Linux (tool.uv.sources)
    "pyyaml>=6.0.3",
]

[dependency-groups]
app = [
    "fastapi>=0.115",
    "jinja2>=3.1",
    "markdown>=3.7",
    "uvicorn>=0.32",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]

[tool.uv]
default-groups = ["dev", "app"]

# En Linux (CI, servidores) torch se instala en su build solo-CPU: los embeddings
# del RAG corren en CPU y la build CUDA añade varios GB de librerías nvidia.
[tool.uv.sources]
torch = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux'" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pitchiq"]

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
markers = [
    "network: tests que descargan datos de StatsBomb (excluidos por defecto)",
]
addopts = "-m 'not network'"
